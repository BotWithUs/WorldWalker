#include <cstdio>

// wwbuild — WorldWalker's offline artifact builder.
// Scaffold entry point. Real pipeline (cache decode -> collision -> graph ->
// ALT bake -> serialize) lands in Phase 2.
int main(int argc, char **argv)
{
    (void)argc;
    (void)argv;
    std::printf("wwbuild - WorldWalker offline artifact builder (scaffold; no commands yet)\n");
    return 0;
}
