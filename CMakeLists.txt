# This file is automatically generated from cmake.toml - DO NOT EDIT
# See https://github.com/build-cpp/cmkr for more information

cmake_minimum_required(VERSION 3.15)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_BINARY_DIR)
	message(FATAL_ERROR "In-tree builds are not supported. Run CMake from a separate directory: cmake -B build")
endif()

set(CMKR_ROOT_PROJECT OFF)
if(CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)
	set(CMKR_ROOT_PROJECT ON)

	# Bootstrap cmkr and automatically regenerate CMakeLists.txt
	include(cmkr.cmake OPTIONAL RESULT_VARIABLE CMKR_INCLUDE_RESULT)
	if(CMKR_INCLUDE_RESULT)
		cmkr()
	endif()

	# Enable folder support
	set_property(GLOBAL PROPERTY USE_FOLDERS ON)

	# Create a configure-time dependency on cmake.toml to improve IDE support
	set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS cmake.toml)
endif()

# vcpkg settings
# Match NXTCacheLibrary's triplet so the shared vcpkg binary cache (zlib) is
# reused and the dynamic CRT is kept (required for loading from the JVM later).
set(VCPKG_TARGET_TRIPLET x64-windows-static-md)

if(NOT DEFINED CMAKE_CXX_STANDARD)
    set(CMAKE_CXX_STANDARD 23)
endif()
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

project(WorldWalker
	LANGUAGES
		CXX
	VERSION
		0.1.0
	DESCRIPTION
		"Speed-focused C++ pathfinding library for RuneScape 3 (NXT engine)"
)

if(MSVC)
    add_compile_definitions(NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
    add_compile_options(/W4 /WX /permissive- /Zc:preprocessor /utf-8)
endif()

# wwbuild decodes the RS cache through the NXTCache C ABI (offline only). Import
# the prebuilt DLL + import lib produced by NXTCacheLibrary's `cmake -B build`.
# Override NXTCACHE_BUILD_DIR to point elsewhere; it must contain
# <CONFIG>/NXTCache.{lib,dll}. The DLL must be on PATH (or beside wwbuild.exe)
# at run time.
if(NOT DEFINED NXTCACHE_BUILD_DIR)
    set(NXTCACHE_BUILD_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../NXTCacheLibrary/build")
endif()
if(NOT TARGET NXTCache::NXTCache)
    add_library(NXTCache::NXTCache SHARED IMPORTED)
    set_target_properties(NXTCache::NXTCache PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${CMAKE_CURRENT_SOURCE_DIR}/../NXTCacheLibrary/src"
        IMPORTED_CONFIGURATIONS "DEBUG;RELEASE"
        IMPORTED_IMPLIB_DEBUG     "${NXTCACHE_BUILD_DIR}/Debug/NXTCache.lib"
        IMPORTED_LOCATION_DEBUG   "${NXTCACHE_BUILD_DIR}/Debug/NXTCache.dll"
        IMPORTED_IMPLIB_RELEASE   "${NXTCACHE_BUILD_DIR}/Release/NXTCache.lib"
        IMPORTED_LOCATION_RELEASE "${NXTCACHE_BUILD_DIR}/Release/NXTCache.dll"
        MAP_IMPORTED_CONFIG_RELWITHDEBINFO "Release"
        MAP_IMPORTED_CONFIG_MINSIZEREL "Release")
endif()

if(CMKR_ROOT_PROJECT AND NOT CMKR_DISABLE_VCPKG)
	include(FetchContent)
	# Fix warnings about DOWNLOAD_EXTRACT_TIMESTAMP
	if(POLICY CMP0135)
		cmake_policy(SET CMP0135 NEW)
	endif()
	message(STATUS "Fetching vcpkg (2025.06.13)...")
	FetchContent_Declare(vcpkg
		URL
			"https://github.com/microsoft/vcpkg/archive/refs/tags/2025.06.13.tar.gz"
		SUBBUILD_DIR
			"CMakeFiles/vcpkg-subbuild"
		SOURCE_DIR
			vcpkg
		BINARY_DIR
			"CMakeFiles/vcpkg-build"
	)
	FetchContent_MakeAvailable(vcpkg)

	if(CMAKE_HOST_SYSTEM_NAME STREQUAL Darwin AND CMAKE_OSX_ARCHITECTURES STREQUAL "")
		set(CMAKE_OSX_ARCHITECTURES ${CMAKE_HOST_SYSTEM_PROCESSOR} CACHE STRING "" FORCE)
	endif()
	include("${vcpkg_SOURCE_DIR}/scripts/buildsystems/vcpkg.cmake")
endif()

# Packages
find_package(ZLIB REQUIRED)

find_package(nlohmann_json REQUIRED)

# Target: worldwalker
set(worldwalker_SOURCES
	cmake.toml
	"src/c_api/worldwalker_c.cpp"
	"src/c_api/worldwalker_c.h"
	"src/data/DatasetLoader.cpp"
	"src/data/DatasetLoader.h"
	"src/data/TransitionCost.h"
	"src/data/Transitions.h"
	"src/exec/Callbacks.h"
	"src/exec/Executor.cpp"
	"src/exec/Executor.h"
	"src/format/Artifact.h"
	"src/format/ArtifactReader.cpp"
	"src/format/ArtifactReader.h"
	"src/format/ClipFlags.h"
	"src/format/WallApproach.h"
	"src/format/Zlib.cpp"
	"src/format/Zlib.h"
	"src/runtime/AltHeuristic.cpp"
	"src/runtime/AltHeuristic.h"
	"src/runtime/AreaSearch.cpp"
	"src/runtime/AreaSearch.h"
	"src/runtime/CapabilitySnapshot.h"
	"src/runtime/ContextPool.cpp"
	"src/runtime/ContextPool.h"
	"src/runtime/InstanceMap.cpp"
	"src/runtime/InstanceMap.h"
	"src/runtime/PathAssembler.cpp"
	"src/runtime/PathAssembler.h"
	"src/runtime/RuntimeTeleports.cpp"
	"src/runtime/RuntimeTeleports.h"
	"src/runtime/SearchContext.h"
	"src/runtime/TeleportPolicy.h"
	"src/runtime/TileScan.h"
	"src/runtime/TileSearch.cpp"
	"src/runtime/TileSearch.h"
	"src/runtime/WorldView.cpp"
	"src/runtime/WorldView.h"
)

add_library(worldwalker SHARED)

target_sources(worldwalker PRIVATE ${worldwalker_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${worldwalker_SOURCES})

target_compile_definitions(worldwalker PUBLIC
	WORLDWALKER_BUILDING
)

target_compile_features(worldwalker PUBLIC
	cxx_std_23
)

target_include_directories(worldwalker PUBLIC
	src
)

target_link_libraries(worldwalker PUBLIC
	ZLIB::ZLIB
	nlohmann_json::nlohmann_json
)

set_target_properties(worldwalker PROPERTIES
	WINDOWS_EXPORT_ALL_SYMBOLS
		ON
)

# Target: wwbuild
set(wwbuild_SOURCES
	cmake.toml
	"src/build/AltLandmarks.cpp"
	"src/build/AreaGraph.cpp"
	"src/build/ArtifactWriter.cpp"
	"src/build/CacheClient.cpp"
	"src/build/CollisionBuilder.cpp"
	"src/build/CollisionLookup.cpp"
	"src/build/main.cpp"
	"src/data/CrossingDeriver.cpp"
	"src/data/FreshnessDeriver.cpp"
	"src/data/TeleportZones.cpp"
	"src/data/TransitionBuilder.cpp"
)

add_executable(wwbuild)

target_sources(wwbuild PRIVATE ${wwbuild_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${wwbuild_SOURCES})

target_compile_features(wwbuild PRIVATE
	cxx_std_23
)

target_include_directories(wwbuild PRIVATE
	src
)

target_link_libraries(wwbuild PRIVATE
	worldwalker
	NXTCache::NXTCache
	nlohmann_json::nlohmann_json
)

get_directory_property(CMKR_VS_STARTUP_PROJECT DIRECTORY ${PROJECT_SOURCE_DIR} DEFINITION VS_STARTUP_PROJECT)
if(NOT CMKR_VS_STARTUP_PROJECT)
	set_property(DIRECTORY ${PROJECT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT wwbuild)
endif()

# Target: wwcli
set(wwcli_SOURCES
	cmake.toml
	"src/cli/Bench.cpp"
	"src/cli/CrossCheck.cpp"
	"src/cli/DoorPaths.cpp"
	"src/cli/InstanceTests.cpp"
	"src/cli/PathExport.cpp"
	"src/cli/ScriptedPaths.cpp"
	"src/cli/WallShapeTests.cpp"
	"src/cli/main.cpp"
)

add_executable(wwcli)

target_sources(wwcli PRIVATE ${wwcli_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${wwcli_SOURCES})

target_compile_features(wwcli PRIVATE
	cxx_std_23
)

target_include_directories(wwcli PRIVATE
	src
)

target_link_libraries(wwcli PRIVATE
	worldwalker
	nlohmann_json::nlohmann_json
)

get_directory_property(CMKR_VS_STARTUP_PROJECT DIRECTORY ${PROJECT_SOURCE_DIR} DEFINITION VS_STARTUP_PROJECT)
if(NOT CMKR_VS_STARTUP_PROJECT)
	set_property(DIRECTORY ${PROJECT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT wwcli)
endif()
